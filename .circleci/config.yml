version: 2
jobs:
  build:
    working_directory: ~/search-widget-react-redux
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test
