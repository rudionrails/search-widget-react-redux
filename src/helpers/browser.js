/* global window, document, history, location, addEventListener */
/* global removeEventListener, fetch, Event */
import 'whatwg-fetch';

export {
  window,
  document,
  history,
  location,
  fetch,

  addEventListener,
  removeEventListener,
};
