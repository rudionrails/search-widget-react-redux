/* global window, document, history, location, */
/* global addEventListener, removeEventListener */
/* global setTimeout */

import 'whatwg-fetch';

export {
  window,
  document,
  history,
  location,
  fetch,

  addEventListener,
  removeEventListener,
  setTimeout,
};
